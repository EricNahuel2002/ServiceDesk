# Base URL de la API.
# En desarrollo local se recomienda NO configurar esta variable y usar el
# proxy de Vite (vite.config.ts) que redirige /api a http://localhost:5216.
# Solo configurala cuando la API este en otra direccion (ej. Azure).
VITE_API_BASE_URL=http://localhost:5216/api
